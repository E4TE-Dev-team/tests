# tests
DogeUB

On codespace creation run
```bash
git submodule init; git submodule update; curl -fsSL https://tailscale.com/install.sh | sh; sudo tailscaled && sudo tailscale up
```
Then just run:
```bash
./start.sh
```
